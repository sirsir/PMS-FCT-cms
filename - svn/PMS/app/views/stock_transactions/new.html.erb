<%
form_id = "frm_new_#{@stock.id}"
%>
<% form_tag url_for(:action => 'create' ), :onSubmit => "fncPrepareFormForSubmit(this);", :id => form_id, :target=>"tmp_frame" do %>
  <%=screen_creation_tag "#{@stock.label.descr} : #{@transaction_type.titleize}", render(:partial => 'form', :area_id =>""), "#{form_id}_content" %>
<% end %>

<hr />

<%=group_tag "#{@transaction_type.titleize} Transaction", render(:partial => 'transactions'), {:type => 'form', :blindable => false} %>
