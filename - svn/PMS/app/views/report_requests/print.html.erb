<%=render :partial => 'form', :locals => {:read_only => true}%>

<table>
    <tr>
      <td class='td_cell' id="chart_area">
        <%=image_tag url_for(:action => 'chart_image', :id => @report_request.id, :time => Time.now) %>
      </td>
    </tr>
</table>

<%=render :partial => 'table', :locals => {:expand_all_level => true}%>